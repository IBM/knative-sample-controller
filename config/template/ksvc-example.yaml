apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: example-foo
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: $DOCKER_REGISTRY/$DOCKER_USER/sample-controller
          serviceAccountName: example-foo-reconcile